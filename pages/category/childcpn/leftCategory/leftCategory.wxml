
  <scroll-view scroll-y class="scroll">
    <view class="leftCate">
      <block wx:for="{{categoryTitle}}" wx:key="*this">
        <view class="leftCateItem {{index === currentIndex ? 'active': ''}}"
              bind:tap="currenClick" data-index="{{index}}">
        {{item}}
        </view>
      </block>
    </view>
  </scroll-view>



